#!/bin/bash

if [ "$1" = "" ]; then
  echo "require first argument for file name"
  exit
fi

user_name=`git config --get user.name`
current_date=`date "+%Y-%m-%d %H:%M:%S %z"`
cat << EOF > "${user_name}_${1}"
---
layout: post
title:
date: ${current_date}
author: ${user_name}
tags: [read]
---

EOF
